# Only needed for local dev — Vercel injects this automatically in production
# once you create a Blob store and link it to your project.
# Get it from: Vercel Dashboard -> your project -> Storage -> Blob -> .env.local tab
BLOB_READ_WRITE_TOKEN=
